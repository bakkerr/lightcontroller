#include "specialbuttons.h"

/*
specialButtons::specialButtons(QWidget *parent) :
    QWidget(parent)
{
    QHBoxLayout *l0 = new QHBoxLayout();

    groupbox = new QGroupBox(tr("Special Buttons:"));
    groupbox->setMinimumWidth(150);

    QVBoxLayout *l1 = new QVBoxLayout();

    allRandomButton = new QPushButton(tr("All Random"));
    connect(allRandomButton, SIGNAL(clicked()), this, SIGNAL(allRandom()));
    l1->addWidget(allRandomButton);

    allFadeButton = new QPushButton(tr("Check Fade 1-4"));
    connect(allFadeButton, SIGNAL(clicked()), this, SIGNAL(allFade()));
    l1->addWidget(allFadeButton);

    groupbox->setLayout(l1);

    l0->addWidget(groupbox);

    this->setLayout(l0);
}

specialButtons::~specialButtons(){

    delete groupbox;
}
*/
